{
  "name": "postman-clone",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn dev:api\" \"yarn dev:web\"",
    "dev:api": "yarn workspace @postman-clone/api start:dev",
    "dev:web": "yarn workspace @postman-clone/web dev",
    "build": "yarn build:packages && yarn workspace @postman-clone/api build && yarn workspace @postman-clone/web build",
    "build:packages": "yarn workspace @postman-clone/shared-types build && yarn workspace @postman-clone/shared-utils build",
    "typecheck": "yarn typecheck:packages && yarn workspace @postman-clone/api typecheck && yarn workspace @postman-clone/web typecheck",
    "typecheck:packages": "yarn workspace @postman-clone/shared-types typecheck && yarn workspace @postman-clone/shared-utils typecheck",
    "lint": "eslint . --ext .ts,.tsx",
    "seed": "yarn workspace @postman-clone/api seed"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.31.0",
    "@typescript-eslint/parser": "^8.31.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.25.1",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "typescript": "^5.8.3"
  }
}
