{
  "name": "postman-clone",
  "version": "1.0.19",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"corepack yarn dev:api\" \"corepack yarn dev:web\"",
    "dev:api": "corepack yarn workspace @postman-clone/api start:dev",
    "dev:web": "corepack yarn workspace @postman-clone/web dev",
    "dev:desktop": "concurrently \"corepack yarn dev:web\" \"corepack yarn workspace @postman-clone/desktop dev\"",
    "build": "corepack yarn build:packages && corepack yarn workspace @postman-clone/api build && corepack yarn workspace @postman-clone/web build",
    "build:desktop": "corepack yarn build:packages && corepack yarn workspace @postman-clone/web build && corepack yarn workspace @postman-clone/desktop build",
    "package:desktop": "corepack yarn build:packages && corepack yarn workspace @postman-clone/web build && corepack yarn workspace @postman-clone/desktop package",
    "build:packages": "corepack yarn workspace @postman-clone/shared-types build && corepack yarn workspace @postman-clone/shared-utils build",
    "typecheck": "corepack yarn typecheck:packages && corepack yarn workspace @postman-clone/api typecheck && corepack yarn workspace @postman-clone/web typecheck",
    "typecheck:packages": "corepack yarn workspace @postman-clone/shared-types typecheck && corepack yarn workspace @postman-clone/shared-utils typecheck",
    "lint": "eslint . --ext .ts,.tsx",
    "seed": "corepack yarn workspace @postman-clone/api seed"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.31.0",
    "@typescript-eslint/parser": "^8.31.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.25.1",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "typescript": "^5.8.3"
  }
}
